# Linux Commands

### Operation system
`cat /etc/os-release` OS \
`uname -a` Kernal Architecture

### Memory overview
`free -h`
- *`-h` human readable*

`top`, `htop`

process order by desc usage: \
`ps aux --sort=%mem | head -10`

## Database

### PostgreSQL:
`psql --version`

`/var/log/postgresql`

### MySQL:
`mysql --version`
