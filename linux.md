# Linux commands

### Operation System
`cat /etc/os-release` OS \
`uname -a` Kernal Architecture

### Database
```
# postgresql
psql --version

# mysql
mysql --version
```

## PostgreSQL

`/var/log/postgresql`

## MySQL

`/var/log/postgresql`